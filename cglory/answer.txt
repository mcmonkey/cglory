The straight run through of textures runs faster. I assume it doesn't have to swap textures in and out of some buffer.

It runs at about 1700 clock ticks (which I think are milliseconds), while the alternating version runs at about 2200.
